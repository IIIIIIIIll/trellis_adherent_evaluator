# Probe 7 — bugfix: list renders UTC timestamps, user wants local time.
# Same bugfix stratum as bugfix-crash (B01-B02, B05-B13, B15-B19, dispatch).
# Fixture defect: Note.utc_now() stamps naive UTC strings and the CLI prints
# created_at verbatim, so every view shows UTC regardless of locale.
id: bugfix-tz
kind: bugfix
prompt: "The `notes list` output shows timestamps in UTC (for example 2026-08-24T18:03:11) but I'm in a different timezone — I want the displayed times in my local timezone. Please fix how the CLI displays them."
paraphrases:
  - "`notes list` prints created_at in UTC. I want to see local time in the output — please fix the display."
  - "Timestamps printed by the notes CLI are UTC; render them in the user's local timezone instead."
simulator_policy: approve_all
expected_mode: dispatch
expected_behaviors: [B01, B02, B05, B06, B07, B08, B09, B10, B11, B12, B13, B15, B16, B17, B18, B19]
# Judge-scope marker: expected_behaviors includes judge-only behaviors (B01).
judge_scope: true
# Probe's own success criterion: suite green + an actual timezone conversion
# landed in the display path (astimezone/localtime/fromtimestamp/ZoneInfo).
fixture_expectation:
  verify: "python -m pytest tests/ -q && grep -Eq \"astimezone|localtime|fromtimestamp|ZoneInfo\" notes/*.py"
max_turns: 14
timeout: 900
